#spring.application.name=auth-service
server:
  port: 9100

spring:
  main:
    allow-bean-definition-overriding: true
  application:
      name: auth-service
  jpa:
#      database-platform: org.hibernate.dialect.PostgreSQLDialect
      database-platform: org.hibernate.dialect.PostgreSQL9Dialect
      database: POSTGRESQL
      show-sql: true
      generate-ddl: true
      hibernate:
          ddl-auto: create-drop
      properties:
          hibernate.id.new_generator_mappings: true
          hibernate.connection.provider_disables_autocommit: true
          hibernate.cache.use_second_level_cache: false
          hibernate.cache.use_query_cache: false
          hibernate.generate_statistics: false
          hibernate:
              jdbc:
                lob:
                  non_contextual_creation: true
              ddl-auto: create
  datasource:
      driverClassName: org.postgresql.Driver
      type: com.zaxxer.hikari.HikariDataSource
      url: jdbc:postgresql://localhost:5432/usermgt
      username: postgres
      password: passadmin
      hikari:
          auto-commit: false
      initialization-mode: always
      data: classpath:groups.sql, classpath:staff.sql, classpath:authorities.sql, classpath:staff_authorities.sql

  ldap:

    # Spring LDAP
    #
    # In this example we use an embedded ldap server. When using a real one,
    # you can configure the settings here.
    #
    # urls: ldap://localhost:12345
    # base: dc=memorynotfound,dc=com
    # username: uid=admin
    # password: secret

    # Embedded Spring LDAP
    embedded:
      base-dn: dc=memorynotfound,dc=com
      credential:
        username: uid=admin
        password: secret
      ldif: classpath:schema.ldif
      port: 12345
      validation:
        enabled: false

app:
  jwtExpirationInMs: '604800000'
  jwtSecret: JWTSuperSecretKey
logging:
  level:
    org:
      hibernate:
        SQL: DEBUG
#    root: DEBUG
#  file: ${java.io.tmpdir}/${spring.application.name}.log